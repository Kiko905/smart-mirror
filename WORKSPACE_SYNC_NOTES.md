# Workspace Sync Notes

This SSH workspace currently contains only the `pi-client` part of Smart Mirror.

Missing directories in this checkout:
- backend
- frontend

To complete backend and frontend integration commit, sync or clone full repository contents into this same git worktree.
